
const userProfileSource = document.getElementById('user-profile-template').innerHTML;

const userProfileTemplate = Handlebars.templates.userProfile(userProfileSource);

<!-- Need to google how to precompile without a known context (API response would be known) -->
const userProfilePlaceholder = document.getElementById('user-profile');
userProfilePlaceholder.innerHTML = userProfileTemplate({context});
